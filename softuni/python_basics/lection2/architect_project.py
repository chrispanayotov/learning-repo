# Calcuate the time needed for an architect to complete his project
name = input()
projects_counts = int(input())
hours = projects_counts * 3

print(f"The architect {name} will need {hours} hours"
      f" to complete {projects_counts} project/s.")
